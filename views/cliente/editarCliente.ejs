<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Cliente</title>
    <!-- Link Bootstrap -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <!-- CSS Específico -->
    <link rel="stylesheet" href="/public/forms.css">
</head>
<body>
    <div class="container col-11 col-sm-6 col-md-6 col-lg-4">
        <h1 class="text-center mb-4">Editar Cliente</h1>
        <form action="/mecanico/cliente/<%= cliente.id %>?_method=PATCH" method="POST" class="row g-3">
            <!-- Nome -->
            <div class="col-md-6">
                <label for="nome" class="form-label">Nome:</label>
                <input type="text" id="nome" name="nome" class="form-control" value="<%= cliente.nome %>" required>
            </div>
            
            <!-- Telefone -->
            <div class="col-md-6">
                <label for="telefone" class="form-label">Telefone:</label>
                <input type="text" id="telefone" name="telefone" class="form-control" value="<%= cliente.telefone %>" required>
            </div>
            
            <!-- Email -->
            <div class="col-12">
                <label for="email" class="form-label">Email:</label>
                <input type="email" id="email" name="email" class="form-control" value="<%= cliente.email %>" required>
            </div>
            
            <!-- Endereço -->
            <div class="col-12">
                <label for="endereco" class="form-label">Endereço:</label>
                <input type="text" id="endereco" name="endereco" class="form-control" value="<%= cliente.endereco %>" required>
                <br>
            </div>
            
            <!-- Botão de salvar -->
            <div class="col-12 text-center">
                <button type="submit" class="botao">Salvar</button>
            </div>
        </form>
        <br><br>
        <div class="d-flex justify-content-end">
            <a href="/mecanico/clientes" class="btn btn-danger">Voltar</a>
        </div>
    </div>
</body>
</html>